<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="ISDCompanion.CoursesPage">
    <ContentPage.Content>
        <CollectionView x:Name="courses"
                        SelectionMode="Single"
                        SelectionChanged="OnCollectionViewSelectionChanged">
        <CollectionView.Header>
            <SearchBar x:Name="MainSearchBar" Placeholder="Search term" 
                       TextChanged="OnSearchTextChanged"/>
        </CollectionView.Header>
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout Orientation="Horizontal"                                 
                                 VerticalOptions="FillAndExpand"
                                         Padding="20,8,20,8">
                    <Label Text="{Binding Semester}" FontSize="Large"/>
                <StackLayout Orientation="Vertical">
                    <Label Text="{Binding Name}" FontAttributes="Bold" />
                    <Label Text="{Binding Lecturer}" FontAttributes="Italic"
                           VerticalOptions="End" />
                </StackLayout>
                    </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
    </ContentPage.Content>
</ContentPage>

