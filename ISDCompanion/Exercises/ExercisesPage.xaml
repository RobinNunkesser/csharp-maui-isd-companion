<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
    xmlns:local="clr-namespace:ISDCompanion"   
    x:Class="ISDCompanion.ExercisesPage"
    Title="{xct:Translate Exercises}">
    <ContentPage.Content>
        <TableView Intent="Menu">
            <TableRoot>
                <TableSection Title="{xct:Translate Networks}">
                    <TextCell Text="{xct:Translate Bitencodings}" Command="{Binding NavigateCommand}" CommandParameter="{x:Type local:BitencodingsPage}" />                    
                </TableSection>
                <TableSection Title="{xct:Translate OperatingSystems}">
                     <TextCell Text="{xct:Translate PageReplacement}" Command="{Binding NavigateCommand}" CommandParameter="{x:Type local:PageReplacementPage}" />
                </TableSection>
            </TableRoot>
        </TableView>            
    </ContentPage.Content>
</ContentPage>
